const initialState = {
    list :[]
}
export const DanceReducer = (state = initialState, action) => {
    let {type, payload} = action;
    switch(type){

        default:
            return state;
    }
}